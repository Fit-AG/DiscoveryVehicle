<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:background="#fff">

    <Button
        android:id="@+id/btAction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btActionConnect"
        android:background="@color/primary"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:id="@+id/tvWaterDepth"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tvWaterDepthNotFound"
        android:enabled="false"
        android:layout_above="@+id/tvConnectionState"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/tvConnectionState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/primaryText"
        android:text="@string/connDisconnected"
        android:layout_above="@+id/btAction"
        android:layout_centerHorizontal="true" />

    <ImageView
        android:id="@+id/ivLogo"
        android:layout_width="60dp"
        android:layout_height="90dp"
        android:src="@drawable/ohg_logo"
        android:layout_alignParentRight="true"
        android:layout_marginRight="20dp"
        android:layout_above="@+id/btAction" />

    <ImageView
        android:id="@+id/ivWaterFound"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/water_drop_inactive"
        android:layout_above="@+id/tvConnectionState"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

</RelativeLayout>
